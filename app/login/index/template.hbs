<div class="login-container layout-row layout-wrap layout-align-center-center">
  {{#if session.isAuthenticated}}
    <p>You have logged in successfully.</p>
    {{outlet}}
  {{else}}
    {{login-form}}
  {{/if}}
</div>
