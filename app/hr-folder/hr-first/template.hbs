{{#paper-tabs
  center=true
  stretch=true
  borderBottom=true
  selected=selectedBasicTab
  class="settings-menu"
  onChange=(action (mut selectedBasicTab)) as |tabs|}}
  <div class="settings-menu__container layout-column layout-gt-sm-row">
    {{#tabs.tab class='settings-menu__items'}}Corporate Birthday{{/tabs.tab}}
    {{#tabs.tab class='settings-menu__items'}}Child's day{{/tabs.tab}}
    {{#tabs.tab class='settings-menu__items'}}Corporate Teambuilding{{/tabs.tab}}
    {{#tabs.tab class='settings-menu__items'}}Christmas Party{{/tabs.tab}}
    {{#tabs.tab class='settings-menu__items'}}CSR{{/tabs.tab}}
  </div>
{{/paper-tabs}}

{{#liquid-bind (hash tab=selectedBasicTab) as |current|}}
  {{#if (eq current.tab 0)}}
    {{#paper-card as |card|}}
      Corporate Birthday
    {{/paper-card}}
  {{/if}}
  {{#if (eq current.tab 1)}}
    {{#paper-card as |card|}}
      Child's day
    {{/paper-card}}
  {{/if}}
  {{#if (eq current.tab 2)}}
    {{#paper-card as |card|}}
      Corporate Teambuilding
    {{/paper-card}}
  {{/if}}
  {{#if (eq current.tab 3)}}
    {{#paper-card as |card|}}
      Christmas Party
    {{/paper-card}}
  {{/if}}
  {{#if (eq current.tab 4)}}
    {{#paper-card as |card|}}
      CSR
    {{/paper-card}}
  {{/if}}

{{/liquid-bind}}

<div class="infoDetails"><p>All the info related to both corporate events and CSR projects will be shared here. If we start a charity campaign or work for the  organization of a party you can always check  the progress. The annual events calendar will also be situated in this folder. </p></div>
